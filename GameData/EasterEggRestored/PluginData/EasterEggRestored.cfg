EASTER_EGG_RESTORED
{
    // Vallhenge / Vall Icehenge stock PQSCity.
    // Moved from original stock location to a slightly flatter place 50m away, and raised above terrain.
    STATIC_RESTORE
    {
        body = Vall
        city = Icehenge
        repositionRadial = 16129.197898,-261440.043326,149606.476915
        repositionRadiusOffset = 1650.700669
        repositionToSphere = true
        repositionToSphereSurface = false
        repositionToSphereSurfaceAddHeight = false
        reorientToSphere = true
        reorientInitialUp = 0,1,0
        reorientFinalAngle = 85.286652
        applyAllMatches = true
    }

    // Dres red sports car / Anniversary6.
    // Only apply when Proximas Better Dres is installed.
    // I just raised it up to the new terrain height of Better Dres.
    STATIC_RESTORE
    {
        body = Dres
        city = Anniversary6
        needsFolder = BetterDres
        repositionRadial = 62332.502044,4875.700765,128594.722447
        repositionRadiusOffset = 4988.516708
        repositionToSphere = true
        repositionToSphereSurface = false
        repositionToSphereSurfaceAddHeight = false
        reorientToSphere = true
        reorientInitialUp = 0,1,0
        reorientFinalAngle = 260
        applyAllMatches = true
    }

    // Eeloo Snowkerbal / Anniversary3 for OPM/Outer Planets Mod.
    // Only apply when Outer Planets Mod is installed.
    // I put it in the same spot it was at, just at new Eeloo.
    STATIC_RESTORE
    {
        body = Eeloo
        city = Anniversary3
        needsAnyFolder = OPM,OuterPlanetsMod
        repositionRadial = -66997.771400,92899.694425,178318.901438
        repositionRadiusOffset = 1935.568527
        repositionToSphere = true
        repositionToSphereSurface = false
        repositionToSphereSurfaceAddHeight = false
        reorientToSphere = true
        reorientInitialUp = 0,1,0
        reorientFinalAngle = 260
        applyAllMatches = true
    }
}
